services:
  - type: web
    name: veritasai-forensics
    runtime: python
    rootDir: forensics
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120
