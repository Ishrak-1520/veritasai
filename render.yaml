services:
  - type: web
    name: veritasai
    runtime: node
    buildCommand: npm install
    startCommand: node server/index.js
    envVars:
      - key: FFMPEG_PATH
        value: /usr/bin/ffmpeg
      - key: FFPROBE_PATH
        value: /usr/bin/ffprobe